export enum StatusCodes {
  OK = 200,
  BadRequest = 400,
  Unauthorized = 401,
  Forbidden = 403,
  NotFound = 404,
}

export enum Codes {
  Success = 'success',
  BadRequest = 'bad_request',
  Forbidden = 'forbidden',
  NotFound = 'not_found',
}
